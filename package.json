{
  "name": "chitchat-web",
  "version": "2.0.0",
  "description": "ChitChat - Secure P2P Encrypted Messaging Web Application",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd client && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd backend && npm install && cd ../client && npm install",
    "build:client": "cd client && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/chitchat-web.git"
  },
  "keywords": [
    "chat",
    "messaging",
    "p2p",
    "encrypted",
    "secure",
    "react",
    "tailwind"
  ],
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

[phases.setup]
nixPkgs = ["nodejs", "npm"]
cmds = [
  "npm install"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "node backend/server-new.js"

[health]
cmd = "curl -f http://localhost:3000/health || exit 1"
interval = 30
timeout = 10
startPeriod = 10
